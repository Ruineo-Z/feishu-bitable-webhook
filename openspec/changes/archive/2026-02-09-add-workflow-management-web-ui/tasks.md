## 1. UI 入口与页面骨架

- [x] 1.1 新增工作流管理页面路由（如 `/ui/workflows`）并确保服务可访问
- [x] 1.2 建立页面基础结构（列表区、编辑区、状态提示区）与基础样式
- [x] 1.3 拆分前端脚本模块（API 调用、状态管理、视图渲染）以便后续维护

## 2. 工作流列表与分页

- [x] 2.1 封装 `GET /api/workflows` 请求并解析 `code/message/data/meta` 响应
- [x] 2.2 渲染工作流摘要列表（名称、启用状态、scope、更新时间）
- [x] 2.3 实现分页参数控制（limit/offset）与分页切换后的列表刷新
- [x] 2.4 实现列表加载中、加载失败、空数据三种状态展示

## 3. 创建与编辑工作流

- [x] 3.1 实现“新建工作流”表单并接入 `POST /api/workflows`
- [x] 3.2 实现 scope 表单校验：`table` 必填 `appToken/tableId`，`global` 不提交表绑定
- [x] 3.3 实现 DSL JSON 字段语法校验，阻止非法 JSON 提交
- [x] 3.4 实现“编辑工作流”流程：先拉取 `GET /api/workflows/{id}` 再提交 `PUT /api/workflows/{id}`
- [x] 3.5 更新成功后刷新列表与当前表单状态，确保展示与后端一致

## 4. 删除与反馈机制

- [x] 4.1 实现删除确认交互，确认后调用 `DELETE /api/workflows/{id}`
- [x] 4.2 删除成功后从当前列表移除目标项，取消删除时保持列表不变
- [x] 4.3 统一成功/失败提示：展示后端 `message`，失败时同时显示 `code`
- [x] 4.4 对常见错误场景提供友好文案（如对象不存在、参数错误、服务异常）

## 5. 文档与验收

- [x] 5.1 在 README 补充管理页面访问路径与使用说明
- [x] 5.2 手工验证工作流 CRUD 全流程（列表、创建、编辑、删除）
- [x] 5.3 验证 `table/global` 作用域配置与错误提示行为符合 specs
