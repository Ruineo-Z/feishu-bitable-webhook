## 1. 数据模型与迁移

- [x] 1.1 为 `workflows` 增加 `scope_type`、`app_token`、`table_id` 字段并创建路由索引
- [x] 1.2 编写历史数据回填脚本（legacy trigger → table/global scope）
- [x] 1.3 输出迁移审计结果（global 回填清单、异常记录）

## 2. 路由查询与执行入口改造

- [x] 2.1 在工作流查询层新增按 scope 的候选检索方法
- [x] 2.2 在事件入口将匹配流程改为“先 scope 路由，再条件评估与执行”
- [x] 2.3 保留过渡期兼容兜底并增加可观测日志（候选数量、命中来源）

## 3. API 与配置约束

- [x] 3.1 扩展工作流创建/更新接口，支持显式 scope 入参
- [x] 3.2 增加校验规则：`table` 必须绑定 app/table，`global` 必须显式声明
- [x] 3.3 更新 Swagger 文档中的 scope 字段说明与示例

## 4. 测试与验收

- [x] 4.1 增加路由匹配测试（table/global/混合候选）
- [x] 4.2 增加迁移验证测试（历史数据回填正确性）
- [x] 4.3 执行手工联调并确认性能与触发行为符合预期
