## 1. 运行时字段类型上下文注入

- [x] 1.1 在事件执行链路中补充可复用的字段类型映射构建逻辑（按字段名/字段 ID）
- [x] 1.2 在 `workflow core engine` 的条件评估上下文中注入 `fieldTypes`，替换当前空对象占位
- [x] 1.3 在 `condition` 插件执行路径中注入同样的 `fieldTypes`，确保独立调用路径行为一致

## 2. 条件评估类型归一与回退语义

- [x] 2.1 为条件引擎增加字段类型别名归一（`single_select`/`multi_select` -> `singleSelect`/`multiSelect`）
- [x] 2.2 校准类型处理器选择逻辑，确保已知类型优先命中、未知类型稳定回退文本处理器
- [x] 2.3 固化 `source: before/after` 与 `changed` 在类型感知场景下的行为一致性

## 3. 事件上下文与类型来源一致性

- [x] 3.1 对齐 `record_changed` 事件字段键与字段类型键的对应关系，避免字段名/字段 ID 错配
- [x] 3.2 明确字段类型元信息不可用时的降级路径（不抛错、可执行、可观察）
- [x] 3.3 补充必要日志/诊断信息，便于定位“类型缺失导致回退”的执行情况

## 4. 测试与回归保障

- [x] 4.1 增加条件引擎单测：数字比较、人员包含、多选包含、关联包含、空值判断
- [x] 4.2 增加 source-aware 单测：同一字段在 `before` 与 `after` 下结果差异可验证
- [x] 4.3 增加类型缺失回退单测：验证未知类型回退文本处理器且不中断执行
- [x] 4.4 增加集成测试：真实 workflow 触发上下文中 `fieldTypes` 注入并命中对应处理器

## 5. 文档与验收

- [x] 5.1 更新 API 文档/示例，明确 `condition` 与 `query/delete filter` 操作符差异
- [x] 5.2 在 README 保持字段类型判断矩阵与当前实现一致（含限制项）
- [x] 5.3 执行预发联调用例（人员、数字、多选、空值）并记录与预期一致的验收结果
  - 验收日期：2026-02-21
  - 验收环境：`app_token=IYWEbf7QTawyfhsZgIwc5D26nSe`，`table_id=tblYRgN4gXLN7BTR`
  - 用例 1（正例）：`第一负责人=有值`、`粉丝数>=100`、`标签包含 optJGGP4bs`、`备注为空`、`粉丝数 changed`，结果：`end_pass`（通过）
  - 用例 2（人员反例）：清空 `第一负责人`，结果：`c_owner=false -> end_fail`（符合预期）
  - 用例 3（数字反例）：`第一负责人` 保持有值，`粉丝数=90`，结果：`c_number=false -> end_fail`（符合预期）
  - 用例 4（多选反例）：移除标签 `optJGGP4bs`，结果：`c_tag=false -> end_fail`（符合预期）
  - 用例 5（空值反例）：`备注` 由空改为非空，结果：`c_remark=false -> end_fail`（符合预期）
